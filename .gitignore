# Dados reais de caso NUNCA entram no repositório (LGPD / sigilo profissional).
# Fixtures de teste só podem ser versionadas depois de anonimizadas.
/casos/
/entradas/
/saidas/
*.docx
*.xlsx
*.xls
*.pdf
# Sem exceção para assets/modelos/: o DOCX timbrado sai de peça real do
# escritório e fica na máquina de quem usa. Prepare com
# scripts/preparar_modelo.py; não commite.
!tests/fixtures/**/anon-*.xlsx
!tests/fixtures/**/anon-*.docx

# Pacote da skill: gerado por ./empacotar.sh, não versionado.
/elaborador-inicial.zip

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store
